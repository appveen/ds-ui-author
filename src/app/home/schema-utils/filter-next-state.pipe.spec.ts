import { FilterNextStatePipe } from './filter-next-state.pipe';

describe('FilterNextStatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNextStatePipe();
    expect(pipe).toBeTruthy();
  });
});
