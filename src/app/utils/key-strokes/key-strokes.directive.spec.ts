import { KeyStrokesDirective } from './key-strokes.directive';

describe('KeyStrokesDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyStrokesDirective();
    expect(directive).toBeTruthy();
  });
});
