import { ListenOnTypeDirective } from './listen-on-type.directive';

describe('ListenOnTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new ListenOnTypeDirective();
    expect(directive).toBeTruthy();
  });
});
