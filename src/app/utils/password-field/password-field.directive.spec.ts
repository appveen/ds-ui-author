import { PasswordFieldDirective } from './password-field.directive';

describe('PasswordFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordFieldDirective();
    expect(directive).toBeTruthy();
  });
});
