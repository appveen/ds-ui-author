import { FilterDefinitionPipe } from './filter-definition.pipe';

describe('FilterDefinitionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDefinitionPipe();
    expect(pipe).toBeTruthy();
  });
});
