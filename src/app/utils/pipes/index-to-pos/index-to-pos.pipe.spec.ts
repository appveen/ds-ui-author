import { IndexToPosPipe } from './index-to-pos.pipe';

describe('IndexToPosPipe', () => {
  it('create an instance', () => {
    const pipe = new IndexToPosPipe();
    expect(pipe).toBeTruthy();
  });
});
