import { CommonFilterPipe } from './common-filter.pipe';

describe('CommonFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CommonFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
