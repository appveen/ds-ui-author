import { MakeFocusDirective } from './make-focus.directive';

describe('MakeFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeFocusDirective(null);
    expect(directive).toBeTruthy();
  });
});
