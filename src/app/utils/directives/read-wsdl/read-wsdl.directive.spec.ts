import { ReadWsdlDirective } from './read-wsdl.directive';

describe('ReadWsdlDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadWsdlDirective(null);
    expect(directive).toBeTruthy();
  });
});
