import { AutoLogoutDirective } from './auto-logout.directive';

describe('AutoLogoutDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoLogoutDirective(null);
    expect(directive).toBeTruthy();
  });
});
