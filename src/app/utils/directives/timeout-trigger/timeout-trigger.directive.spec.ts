import { TimeoutTriggerDirective } from './timeout-trigger.directive';

describe('TimeoutTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeoutTriggerDirective(null);
    expect(directive).toBeTruthy();
  });
});
