import { OnChangeDirective } from './on-change.directive';

describe('OnChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new OnChangeDirective();
    expect(directive).toBeTruthy();
  });
});
