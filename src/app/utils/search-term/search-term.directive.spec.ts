import { SearchTermDirective } from './search-term.directive';

describe('SearchTermDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchTermDirective(null);
    expect(directive).toBeTruthy();
  });
});
