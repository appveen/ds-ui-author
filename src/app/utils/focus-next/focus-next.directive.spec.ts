import { FocusNextDirective } from './focus-next.directive';

describe('FocusNextDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusNextDirective(null);
    expect(directive).toBeTruthy();
  });
});
