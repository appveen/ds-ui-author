import { SpaceKeyDirective } from './space-key.directive';

describe('SpaceKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new SpaceKeyDirective();
    expect(directive).toBeTruthy();
  });
});
