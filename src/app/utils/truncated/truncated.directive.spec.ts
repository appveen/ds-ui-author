import { TruncatedDirective } from './truncated.directive';

describe('TruncatedDirective', () => {
  it('should create an instance', () => {
    const directive = new TruncatedDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
