import { UploadImageDirective } from './upload-image.directive';

describe('UploadImageDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadImageDirective();
    expect(directive).toBeTruthy();
  });
});
