import { ScrollEndDirective } from './scroll-end.directive';

describe('ScrollEndDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollEndDirective();
    expect(directive).toBeTruthy();
  });
});
