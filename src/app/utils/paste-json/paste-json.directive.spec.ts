import { PasteJsonDirective } from './paste-json.directive';

describe('PasteJsonDirective', () => {
  it('should create an instance', () => {
    const directive = new PasteJsonDirective();
    expect(directive).toBeTruthy();
  });
});
