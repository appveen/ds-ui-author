import { AgGridFilterPipe } from './ag-grid-filter.pipe';

describe('AgGridFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AgGridFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
